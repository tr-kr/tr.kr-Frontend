import React from 'react';
import MainCarousel from '../components/MainCarousel';

const Home = () => {
    return (
        <div>
            <MainCarousel />
            {/* 다른 컴포넌트들 */}
        </div>
    );
};

export default Home;
